# mcp

Subgraph MCP server. Second Graph track, built on the same subgraph as the
first — near-zero marginal work, which is exactly why it is in scope on day 9
and CRE is the thing that gets cut before it.
