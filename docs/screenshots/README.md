# Console screenshots

Save the three screenshots here with these exact names, and the README picks
them up:

| File | Screen |
| --- | --- |
| `02-register.png` | 02 REGISTER — a registered photograph, PCE 15,358, block 11667252 |
| `03-negative.png` | 03 NEGATIVE — a different camera, PCE 34.7, no record |
| `05-verdict.png` | 05 VERDICT — the four-verdict card and the stage strip |

**Check before committing.** These frames show photographs from an enrolled
body, and `docs/claims.md` is explicit that a published frame is a forgery
kit. Screenshot thumbnails are far smaller than the 1800px quality 80 that
`docs/gates.md` measured at the null, so they are safe by that standard — but
"far smaller" is a claim, and the rule in this repo is to measure it rather
than assume. Score each one before it is pushed.
