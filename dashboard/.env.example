# The Graph. Same deployment verify/ reads (verify/.env.example) -- the
# Colosseum rehearsal registry on Base Sepolia, not the ETHOnline one.
VITE_SUBGRAPH_URL=https://api.studio.thegraph.com/query/1758974/genesis/v0.1.0-base-sepolia

# Block explorer base URL, for linking a body/image id out. Base Sepolia for
# the rehearsal; swap to https://basescan.org once the mainnet deploy lands.
VITE_EXPLORER_URL=https://sepolia.basescan.org
